"use client";

import React from "react";

export default function CompleteSignupPage() {
  // Minimal placeholder page to satisfy Next/TS build. Real implementation
  // lives elsewhere in the app; this keeps generated validator types valid.
  return null;
}
